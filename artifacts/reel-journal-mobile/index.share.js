import { AppRegistry } from "react-native";
import ShareExtension from "./app/share";

// "shareExtension" is the required component name for expo-share-extension
AppRegistry.registerComponent("shareExtension", () => ShareExtension);
